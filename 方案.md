# OpenCode 会话 Web 管理器 — 可行性分析与方案设计

## 1. 可行性结论

**可以实现，但架构比想象中复杂一点。** 核心难点不在于 Web 页面本身，而在于**数据在哪里**。

---

## 2. OpenCode 数据存储方式

### 2.1 数据库位置

所有会话数据存储在本地 SQLite 数据库中：

```
Windows: %USERPROFILE%\.local\share\opencode\opencode.db
Linux:   ~/.local/share/opencode/opencode.db
macOS:   ~/Library/Application Support/opencode/opencode.db
```

### 2.2 核心表结构

**`session` 表** — 存储每次对话的元信息：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | text (PK) | 会话 ID，如 `ses_126a110e...` |
| `project_id` | text | 关联项目 ID |
| `directory` | text | **工作目录路径**（关键字段） |
| `title` | text | 会话标题 |
| `model` | text | 使用的模型 |
| `agent` | text | 使用的 agent |
| `time_created` | integer | 创建时间（Unix 毫秒） |
| `time_updated` | integer | 最后更新时间 |
| `cost` | real | 消耗金额 |
| `tokens_input` | integer | 输入 token 数 |
| `tokens_output` | integer | 输出 token 数 |
| `metadata` | text | JSON 元数据 |

**`message` 表** — 存储每条消息：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | text (PK) | 消息 ID |
| `session_id` | text (FK) | 所属会话 |
| `time_created` | integer | 发送时间 |
| `data` | text | 消息全文（JSON 格式，含 role / content / parts 等） |

**`project` 表** — 项目信息：

| 字段 | 说明 |
|------|------|
| `id` | 项目 ID |
| `worktree` | Git worktree 路径 |
| `vcs` | 版本控制信息（JSON） |

### 2.3 当前数据量

```
会话总数: 70 条
工作目录数: 20 个
```

每个会话包含：目录路径、标题、模型、agent、token 消耗、消息历史等。

---

## 3. 关键挑战

### 挑战一：数据在本地，服务在云端

```
云服务器（Web 页面）  ←→  你的 PC（SQLite 数据库）
                                  ↑
                              不同机器，不能直接访问
```

**解决方案有三种：**

| 方案 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **A: 本地 Web 服务 + 内网穿透** | PC 上起一个 Web 服务器，用 frp/Cloudflare Tunnel 暴露到公网 | 实时读取数据库，架构简单 | 需要 PC 开机时才能用 |
| **B: 本地同步 Agent + 云端 Web** | PC 上跑一个后台程序，定时把 SQLite DB 同步到云服务器 | 云服务器有数据副本，随时可查 | 延迟几秒到几分钟 |
| **C: 纯云端方案** | 在云服务器上也装 opencode，远程工作 | 一切在云端 | 需要云服务器配置 GUI/代理 |

**推荐方案 A（本地 Web + 隧道），最实用。**

### 挑战二：OpenCode SDK 是否需要实例

- 只查看会话历史 → **不需要**启动 OpenCode 进程，直接读 SQLite 即可
- 继续/恢复会话 → 需要 OpenCode 的 API，可以通过 `@opencode-ai/sdk` 编程调用
- 新建会话 → 同样需要 SDK

### 挑战三：手机端使用

- Web 页面需要适配移动端屏幕
- 操作以阅读和简单的文本输入为主，手机完全够用

---

## 4. 方案 A 详细设计（推荐）

### 4.1 架构

```
┌─ 你的手机 ─────────────────────┐
│  浏览器访问 https://xxx.xxx    │
│  (自适应移动端布局)             │
└──────────────┬─────────────────┘
               │ HTTPS
               ▼
┌─ Cloudflare Tunnel / frp ──────┐
│  内网穿透，将本地端口暴露到公网 │
└──────────────┬─────────────────┘
               │
┌─ 你的 PC ──────────────────────┐
│                                │
│  ┌─ Web 服务器 (端口 8765) ──┐ │
│  │  Python Flask 后端         │ │
│  │  + HTML/JS/CSS 前端        │ │
│  │                            │ │
│  │  ├─ 读 opencode.db (只读)  │ │
│  │  ├─ 提供 REST API          │ │
│  │  │  GET  /api/sessions     │ │
│  │  │  GET  /api/sessions/:id │ │
│  │  │  GET  /api/messages/:id │ │
│  │  │  POST /api/session/new  │ │
│  │  │  POST /api/session/:id  │ │
│  │  │  /resume                │ │
│  │  └─ 提供 Web 前端 (SPA)    │ │
│  └────────────────────────────┘ │
│                    ↑            │
│  ┌─ opencode.db ───┘            │
│  │  SQLite 数据库              │ │
│  └──────────────────────────────│
│                                │
│  ┌─ OpenCode (可选) ──────────┐ │
│  │  用于继续/新建会话时需要    │ │
│  │  @opencode-ai/sdk          │ │
│  └────────────────────────────┘ │
└─────────────────────────────────┘
```

### 4.2 Web 页面布局

```
┌──────────────────────────────────┐
│  🔍 搜索会话...          [+ 新建] │
├──────────────┬───────────────────┤
│  最近会话     │  当前会话内容      │
│              │                   │
│  📁 project1 │  ┌───────────┐    │
│    ├ 实现登录  │  │ 用户: ... │    │
│    ├ 修复bug  │  │ AI: ...   │    │
│    └ 优化性能  │  │ 用户: ... │    │
│              │  │ AI: ...   │    │
│  📁 project2 │  └───────────┘    │
│    ├ 添加API  │                   │
│    └ 写文档   │  [输入框...]发送   │
│              │                   │
│  📁 project3 │  工作目录:         │
│    └ 重构     │  E:\projects\xxx  │
│              │  模型: deepseek... │
└──────────────┴───────────────────┘
    左侧栏               主区域
```

### 4.3 功能清单

| 功能 | 说明 | 需要 OpenCode 进程？ |
|------|------|:---:|
| 搜索会话 | 按标题/目录/模型/关键词搜索 | ❌ 仅读 DB |
| 浏览会话列表 | 最近使用的会话，按项目分组 | ❌ 仅读 DB |
| 查看会话详情 | 阅读完整对话历史 | ❌ 仅读 DB |
| 查看会话元信息 | 工作目录、模型、Agent、token 统计、耗时 | ❌ 仅读 DB |
| **继续会话** | 在 Web 上继续对话 | ✅ 需要 SDK |
| **新建会话** | 选择工作目录，开始新对话 | ✅ 需要 SDK |
| 查看工作目录文件 | 浏览当前工作目录的文件树 | ❌ 直接读文件系统 |
| 会话搜索过滤 | 按目录、模型、时间范围过滤 | ❌ 仅读 DB |

### 4.4 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | Python Flask（你云服务器已经在用，熟悉） |
| 前端 | 单页 HTML（Vue.js 或纯 JS，移动端自适应） |
| 数据库 | sqlite3（Python 内置，直接读 opencode.db） |
| 内网穿透 | Cloudflare Tunnel（免费）或 frp |
| SDK 调用 | `@opencode-ai/sdk`（通过 Node.js 子进程或 REST） |

### 4.5 REST API 设计

```python
# 会话列表
GET /api/sessions?q=关键词&dir=路径&limit=20&offset=0
→ { sessions: [{ id, title, directory, model, agent,
                 time_created, time_updated, message_count,
                 cost, tokens_input, tokens_output }], total: 70 }

# 单会话详情
GET /api/sessions/<id>
→ { id, title, directory, project_id, model, agent,
    time_created, time_updated, cost, tokens_input,
    tokens_output, messages: [...] }

# 会话消息
GET /api/sessions/<id>/messages?limit=50&before=<msg_id>
→ { messages: [{ id, role, content, time_created, ... }] }

# 统计信息
GET /api/stats
→ { total_sessions, total_projects, total_cost,
    total_tokens, top_models: [...], top_dirs: [...] }

# 工作目录列表
GET /api/directories
→ { directories: ["E:/projects/xxx", "E:/projects/yyy", ...] }

# 继续会话（需要 SDK）
POST /api/sessions/<id>/resume
Request: { message: "继续帮我改..." }
→ { status: "sent" }

# 新建会话（需要 SDK）
POST /api/sessions/new
Request: { directory: "E:/projects/xxx", message: "..." }
→ { session_id: "ses_xxx" }
```

### 4.6 关于"继续对话"的实现

OpenCode 的 SDK (`@opencode-ai/sdk`) 支持：

```typescript
import { createOpencodeClient } from "@opencode-ai/sdk/v2/client"

const client = createOpencodeClient({ directory: "E:/projects/xxx" })

// 新建会话
const session = await client.session.create()
await client.session.prompt(session.id, { text: "你好" })

// 继续已有会话
await client.session.prompt("ses_xxx", { text: "继续改..." })
```

但这里有个限制：SDK 需要 Node.js 环境，而你的后端是 Python。有两种处理方式：

1. **Node.js 子进程**：Python 调用 Node.js 脚本来执行 SDK 操作
2. **只读模式 + 远程 OpenCode**：Web 只做查看，要续对话就复制 Session ID 到本地 OpenCode 中继续

对于第一版，建议先做**只读模式**（查看会话历史），后续再实现"继续对话"功能。

---

## 5. 分阶段实施计划

### 第一阶段：只读模式 ✅ 已完成

- [x] Python Flask 后端，读取 SQLite 数据库
- [x] 会话列表 API + 搜索/过滤（按目录、模型、关键词）
- [x] 会话详情 API（含消息历史，从 part 表提取内容）
- [x] 统计信息 API（会话数、项目数、Token 消耗）
- [x] 移动端自适应 Web 前端
- [x] 左侧栏按项目分组 + 右侧会话内容区
- [x] 深色主题
- [ ] 部署：本地启动 + Cloudflare Tunnel（待配置）

### 第二阶段：交互模式（2-3 天）

- [ ] Node.js SDK 集成
- [ ] 在 Web 上继续对话
- [ ] 新建会话（选择工作目录）
- [ ] 流式输出（SSE）
- [ ] 工作目录文件浏览

### 第三阶段：增强功能（按需）

- [ ] 会话对比/分支（Fork）
- [ ] Token 消耗统计图表
- [ ] 多用户支持
- [ ] Webhook 通知（新消息推送到手机）

---

## 6. 关于云服务器的角色

你的云服务器在这个方案中**不是必需的**。因为：

- **数据在本地** PC 上
- **Web 服务在本地** 跑更方便
- **云服务器** 只是用来做内网穿透的跳板（如果不用 Cloudflare Tunnel）

如果用 **Cloudflare Tunnel**，完全不需要云服务器：

```bash
# 在 PC 上安装 cloudflared，一条命令
cloudflared tunnel --url http://localhost:8765
# 会生成一个 https://xxx.trycloudflare.com 的公开地址
```

手机直接访问这个地址即可。

如果你**坚持要用云服务器**，也可以把 Node.js 后端部署在云上，然后 PC 上跑一个同步 agent，定时把 SQLite DB 推送到云端 API。但这增加了复杂度。

---

## 7. 技术可行性总结

| 需求 | 可行性 | 备注 |
|------|:------:|------|
| 搜索所有 OpenCode 会话 | ✅ 可行 | 直接读 SQLite，按标题/目录/内容搜索 |
| 按项目分组显示 | ✅ 可行 | session.directory 字段可区分项目 |
| 查看完整对话历史 | ✅ 可行 | message 表存了完整消息 JSON |
| 查看工作目录 | ✅ 可行 | session.directory 字段 |
| 查看 token/模型/Agent | ✅ 可行 | session 表有对应字段 |
| 手机自适应布局 | ✅ 可行 | CSS 媒体查询 + 响应式设计 |
| **继续已有会话** | ⚠️ 部分可行 | 需要 Node.js SDK，第一版可先不做 |
| **新建会话** | ⚠️ 部分可行 | 同上 |
| 数据实时同步 | ✅ 可行 | 直读数据库，写操作时刷新 |
| 公网访问 | ✅ 可行 | Cloudflare Tunnel / frp |

---

## 8. 一句话结论

> **可以做。** 第一版（只读模式）很简单——Python 读 SQLite + Flask Web，一天就能跑起来。
> **继续对话**需要集成 OpenCode SDK（Node.js），功能更完整但工作量也多一些。
>
> 要不要先做第一版试试？
