# OpenCode Session Web 全面优化重构方案

关联主 Issue: https://github.com/caiyilian/opencode-session-web/issues/12

本方案是后续所有小阶段重构的执行手册。核心目标不是一次性大改，而是把当前单体 Flask + 原生 JS 项目逐步重构为有测试、有日志、有清晰边界、能正确理解 OpenCode 事件模型的 Web 会话管理工具。

## 0. 前置调研结论

### 0.1 本地环境

- 本机已安装 OpenCode CLI，版本为 `1.4.1`。
- 本机存在真实会话数据库：`~/.local/share/opencode/opencode.db`。
- 当前数据库包含主要表：`session`、`message`、`part`、`event`、`event_sequence`、`project`、`permission`、`todo` 等。
- 当前 `event` / `event_sequence` 表存在但本机样本为空，因此近期实现不能只依赖事件表；必须保留 `message` / `part` / `session` 增量扫描兜底。

### 0.2 当前项目风险

- `app.py` 超过 1400 行，路由、SQLite 查询、OpenCode 子进程、SSE 生成器、错误解析、文件浏览、Fork、Undo 等逻辑混在一起。
- `static/js/app.js` 超过 1300 行，所有状态、API、SSE、渲染、Provider 管理、对比、统计图表和输入区逻辑混在全局作用域。
- `opencode_monitor.py` 通过轮询 `part` 表里 `step-finish + reason=stop` 发通知，但没有完整会话状态机，容易被工具调用链和多 step 流程影响。
- 本机 OpenCode 1.4.1 数据库的 `session` 表已没有旧版 `cost`、`tokens_input`、`tokens_output` 列，当前 `/api/stats` 和部分列表/统计 SQL 会直接 500。后续第一批代码阶段必须先做 schema 兼容，再谈大规模重构。
- 当前后端大量使用 `print()`，缺少日志级别、文件轮转、结构化上下文和子进程生命周期日志。
- 当前没有自动化测试，任何重构都缺乏回归保护。

### 0.3 OpenCode 事件模型理解

从上游 `anomalyco/opencode` 的 `dev` 分支事件相关代码可见，新版交互流更接近如下模型：

- 运行期事件包含 `message.updated`、`message.part.delta`、`message.part.updated`、`session.status`、`session.error`、`permission.*`、`question.*` 等。
- CLI 交互完成不应只看某个 `part`，更可靠的运行期完成信号是当前会话进入 `session.status.idle`。
- 本项目当前直接读取 SQLite，且本机事件表为空，所以短期要实现两层策略：
  - Web 发起的 OpenCode 子进程：优先使用子进程退出、明确错误、OpenCode 输出流中的终态事件和安静窗口共同判断完成。
  - 外部 CLI 写入数据库：以 `session.time_updated` / `message.time_updated` / `part.rowid` 增量扫描为兜底，构建 per-session 状态机，避免把 `reason=tool-calls` 当作完成。
- `step-finish + reason=stop` 是重要信号，但不是唯一信号；判断“对话完成”时必须确认没有更新的 `tool-calls` step、没有未完成工具 part、没有同一 assistant 消息继续追加的新 part。

## 1. 目标架构

### 1.1 后端目标结构

逐步从单文件 `app.py` 迁移到如下结构：

```text
opencode_web/
  __init__.py
  app_factory.py
  config.py
  logging_config.py
  db.py
  repositories/
    sessions.py
    messages.py
    parts.py
    projects.py
  services/
    session_reader.py
    opencode_events.py
    opencode_runner.py
    process_manager.py
    sync_watcher.py
    notification_state.py
  routes/
    sessions.py
    stream.py
    stats.py
    files.py
    models.py
  schemas.py
  sse.py
tests/
  unit/
  integration/
```

迁移原则：

- 每个阶段只移动一个边界，不同时重写行为。
- 所有老 API 路径保持兼容，前端迁移完成前不改公共响应字段。
- 对 SQLite 访问集中到 repository 层，业务判断放到 service 层。
- OpenCode 子进程统一交给 `ProcessManager` 管理，所有退出路径都能清理。

### 1.2 前端目标结构

建议引入 **React + TypeScript + Vite**。

选择理由：

- 当前 UI 是典型 SPA：会话列表、过滤、详情、统计、对比、弹窗、流式输出、实时同步都适合组件化。
- React 生态中 SSE、状态管理、测试、语法高亮、Markdown 渲染、虚拟列表都有成熟选择。
- TypeScript 能约束 API response、SSE event、message part 数据结构，降低重构时字段误用。
- Vite 构建轻量，适合从 Flask 静态托管平滑迁移。

目标结构：

```text
frontend/
  package.json
  vite.config.ts
  src/
    api/
    app/
    components/
      sidebar/
      session/
      composer/
      tools/
      providers/
      stats/
    hooks/
    stores/
    styles/
    types/
```

前端迁移原则：

- 先保留现有 Flask 页面，新增 Vite 构建产物由 Flask 托管。
- 先迁移只读浏览，再迁移继续对话和新建会话。
- 每迁移一个功能，都保留浏览器验收清单和回归测试。

## 2. 对话完成与错误判断策略

### 2.1 Web 发起的 OpenCode 子进程

`opencode run --format json` 的输出需要统一解析为内部事件：

```text
raw JSON line -> OpenCodeEvent -> SSE event -> 前端渲染状态
```

完成条件按优先级判断：

1. 明确错误：stderr/stdout 出现限流、认证、模型不可用、quota、rate limit、API key 等错误时立即发送 `error` SSE，不等待 25 秒。
2. 子进程正常退出且没有活跃工具调用：发送 `done`。
3. 输出流出现终态信息：如果存在新版 `session.status.idle`，以它作为最高置信完成信号。
4. 旧版 part 事件：`step-finish + reason=stop` 只能在同一 assistant turn 没有后续活跃工具调用、进程也没有继续输出时作为完成信号。
5. 安静窗口兜底：长时间无输出时先发送 `heartbeat` / `status`，超过配置超时后发送清晰错误。

### 2.2 外部 CLI 对话同步

实时同步分两层：

- 首选：读取 `event` 表或 OpenCode 可用的全局事件流。
- 兜底：每 1-2 秒扫描 `session.time_updated`、`message.time_updated`、`part.rowid` 增量变化，并通过 Web SSE 广播给所有客户端。

浏览器不需要手动刷新，应能收到：

- 会话列表新增或更新时间变化。
- 当前打开会话的新增 message / part。
- 工具调用状态变化。
- 对话完成状态变化。

### 2.3 微信监控完成判断

微信通知脚本不再直接通知每个 `step-finish + reason=stop`。应改为：

- 记录每个 session 的最后已通知 assistant message。
- 对新增 part 构建 message 状态：
  - `step-start` 表示 turn 活跃。
  - `tool` 且 `state.status != completed` 表示仍在执行。
  - `step-finish reason=tool-calls` 表示等待下一轮工具结果或下一 step。
  - `step-finish reason=stop` 表示候选完成。
- 只有候选完成在一个短暂稳定窗口内没有新 part，且 message 是 assistant，才发通知。

## 3. 通用 GitHub 阶段流程

每个阶段都按以下流程执行：

1. 在 GitHub 创建阶段 Issue，写清范围和验收目标。
2. 从最新 `master` 创建阶段分支，默认命名：`nightt5879/refactor/phase-x-y-topic`。
3. 修改代码或文档。
4. 运行自动化测试。
5. 启动 `python app.py`，用浏览器完整自测本阶段影响面。
6. 提交 commit。
7. Push 分支并创建 PR 到 `master`。
8. 在阶段 Issue 评论：解决内容、测试结果、提交 SHA、PR 链接。
9. Merge PR。
10. Close 阶段 Issue。
11. 删除远程和本地阶段分支。
12. 切回并 pull 最新 `master`。
13. 进入下一阶段。

每个阶段的浏览器基础回归至少覆盖：

- 打开首页能加载会话列表。
- 搜索会话、按目录/模型过滤可用。
- 打开会话详情能看到用户消息、AI 文本、reasoning、工具调用、step 元信息。
- 新建会话弹窗可打开和关闭。
- Provider 管理弹窗可打开、勾选和恢复。
- 删除、撤销、Fork、对比入口不报错。
- 移动端宽度下侧栏和输入区不互相遮挡。

## 4. 分阶段计划

### 阶段 0.1：重构方案与事件模型调研

范围：

- 新增根目录 `优化修改重构方案.md`。
- 记录本地环境、当前风险、OpenCode 事件模型理解和后续阶段计划。

验收：

- 打开 GitHub PR 能看到新增方案文档。
- 文档包含 10-20 个小阶段和每阶段可操作验收目标。

### 阶段 0.2：数据库 schema 兼容和最小 smoke 测试

范围：

- 修复 OpenCode 1.4.1 数据库下缺少 `session.cost`、`session.tokens_input`、`session.tokens_output` 导致的 API 500。
- 增加列存在性检测，对旧版 session 聚合列和新版 part 聚合数据都保持兼容。
- 引入 `pytest`、Flask test client、临时 SQLite fixture。
- 为 `/api/stats`、`/api/sessions`、`/api/sessions/<id>` 建立最小测试。
- 添加 `tests/fixtures`，构造含 text/reasoning/tool/step-finish 的小型数据库。

验收：

- 运行 `python -m pytest` 全部通过。
- 浏览器打开首页能加载真实数据库会话，不再因 `no such column: cost` 报错。
- `/api/stats`、`/api/sessions`、`/api/stats/tokens` 在新版和旧版 fixture 上都返回 200。

### 阶段 0.3：日志系统替换 print

范围：

- 增加 `logging_config.py`。
- 后端日志支持级别、控制台输出、文件输出和轮转。
- 保留启动时关键路径提示。

验收：

- 启动 `python app.py` 后终端和日志文件均出现启动日志。
- 触发一次 API 404 或 OpenCode 错误，日志里有结构化上下文。

### 阶段 1.1：后端 app factory 与配置抽离

范围：

- 引入 `opencode_web.app_factory:create_app()`。
- 抽离 DB 路径、超时时间、日志路径、轮询间隔等配置。
- `app.py` 变成兼容入口。

验收：

- `python app.py 8765` 可启动。
- Flask test client 可通过 `create_app(test_config=...)` 使用临时数据库。

### 阶段 1.2：SQLite 访问层抽离

范围：

- 新增 `db.py` 和 sessions/messages/parts repositories。
- 先迁移只读 API：stats、sessions、session detail、message detail。
- SQL 保持等价，不调整返回字段。

验收：

- pytest 覆盖迁移后的查询。
- 浏览器会话列表、搜索、详情、统计数据与迁移前一致。

### 阶段 1.3：消息 part 解析器与完成状态机

范围：

- 新增 `services/opencode_events.py`。
- 统一解析 text、reasoning、tool、step-start、step-finish、patch、file、agent、subtask。
- 实现 `ConversationState`，区分 `running`、`waiting_for_tools`、`candidate_done`、`done`、`error`。

验收：

- 测试覆盖 `reason=tool-calls` 不会被当成完成。
- 测试覆盖 `reason=stop` 后有稳定窗口才完成。
- 浏览器里工具调用链不会提前停止转圈。

### 阶段 1.4：OpenCode 子进程管理

范围：

- 新增 `ProcessManager`，集中启动、跟踪、终止 OpenCode 子进程。
- 注册 `atexit` 和信号处理，Flask 退出时清理子进程。
- stderr/stdout 读取和超时逻辑从路由中移出。

验收：

- 发起一次继续对话后终止 Flask，确认没有残留 `opencode` 子进程。
- pytest 覆盖进程注册、注销和超时清理。

### 阶段 1.5：SSE 运行流服务化

范围：

- 新增 `opencode_runner.py` 和 `sse.py`。
- `/api/sessions/<id>/stream` 和 `/api/sessions/new` 共用同一事件转换逻辑。
- 所有 SSE 输出使用统一事件名和 JSON payload。

验收：

- 浏览器继续已有会话能看到 streaming text/reasoning/tool/status/done。
- 新建会话能返回 session_id 并自动打开。
- 网络断开或浏览器关闭时后端能停止对应流。

### 阶段 1.6：限流和错误快速反馈

范围：

- 建立错误分类：quota/rate_limit/auth/model_not_found/network/timeout/unknown。
- stderr 一旦匹配明确错误，5 秒内推送友好错误。
- 前端错误显示不再展示 Python dict 字符串。

验收：

- 用模拟子进程测试 quota 错误会立即 SSE `error`。
- 浏览器显示中文友好错误和原始技术细节折叠区。

### 阶段 2.1：实时同步后端广播

范围：

- 新增 `sync_watcher.py`，维护全局 SSE 客户端集合。
- 提供 `/api/events`，向浏览器广播 session/message/part 增量。
- 先用 SQLite 轮询兜底，后续可切换 event 表。

验收：

- 打开两个浏览器窗口，一个窗口继续对话，另一个窗口无需刷新看到会话更新时间变化。
- 在 OpenCode CLI 外部产生新消息后，Web 端会话列表自动更新。

### 阶段 2.2：微信监控状态机重构

范围：

- `opencode_monitor.py` 复用后端事件状态机或迁移到共享模块。
- 增加去重、稳定窗口、错误日志和配置项。
- 保留微信扫码登录流程。

验收：

- 包含工具调用的对话只在最终完成后通知一次。
- 纯文本对话完成后能通知。
- 重启监控脚本不会重复通知历史消息。

### 阶段 3.1：前端构建系统引入

范围：

- 新增 `frontend/`，使用 React + TypeScript + Vite。
- Flask 托管构建产物，开发模式支持 Vite dev server。
- 保留旧页面作为 fallback，直到迁移完成。

验收：

- `npm install`、`npm run build` 成功。
- `python app.py` 能加载新构建产物或 fallback 页面。

### 阶段 3.2：前端 API client 与类型定义

范围：

- 定义 Session、Message、Part、ToolState、SSEEvent 类型。
- 封装 fetch、错误解析、取消请求和重试。
- 为 API client 添加单元测试。

验收：

- 类型检查通过。
- 浏览器加载 sessions/stats/directories/models 不报错。

### 阶段 3.3：React 侧栏和会话浏览迁移

范围：

- 迁移搜索、目录分组、模型过滤、Provider 隐藏。
- 引入可维护的状态 store。
- 保持旧 API。

验收：

- 浏览器能搜索、切换目录、打开会话。
- 移动端侧栏可收起，不遮挡主内容。

### 阶段 3.4：React 消息时间线迁移

范围：

- 迁移消息渲染、reasoning、step 元信息、token/cost 展示。
- 为 long text 和 code block 做稳定布局。

验收：

- 打开包含 text/reasoning/tool 的真实会话，所有 part 都能显示。
- 长路径、长代码和中文内容不溢出。

### 阶段 3.5：输入区、新建会话和流式输出迁移

范围：

- 迁移 composer、模型选择、新建会话弹窗。
- 接入统一 SSE 事件。
- 支持发送中、取消、错误、完成状态。

验收：

- 继续会话能实时显示输出。
- 新建会话能选择目录和模型。
- 错误能友好展示，按钮状态正确恢复。

### 阶段 3.6：工具调用富展示

范围：

- 工具调用卡片支持折叠/展开、状态图标、耗时、输入/输出分区。
- bash/read/write/edit 等常见工具有专门展示。
- 代码和 JSON 使用语法高亮。

验收：

- 打开包含 bash/read/write/edit 的会话，工具卡片可展开查看输入输出。
- 工具执行中、成功、失败状态视觉区分清楚。

### 阶段 3.7：统计、Fork、Undo、删除、对比迁移

范围：

- 迁移统计图、Fork、Undo、删除、会话对比。
- 补齐确认弹窗、空状态、失败状态。

验收：

- 所有原有功能入口可用。
- 对比两个会话能并排显示差异。
- 删除后列表和详情状态自动更新。

### 阶段 3.8：UI/UX 统一打磨

范围：

- 统一颜色、间距、字体层级、按钮、弹窗、空状态和加载状态。
- 参考 Open WebUI、ChatGPT Next Web、Lobe Chat 的信息密度，但避免营销页风格。
- 完善移动端布局。

验收：

- 桌面端第一屏包含侧栏、消息区、输入区，信息密度适中。
- 手机宽度下可完成搜索、打开会话、发送消息。
- 无明显文字重叠、按钮溢出、滚动区域错乱。

### 阶段 4.1：端到端测试

范围：

- 引入 Playwright。
- 使用临时 SQLite fixture 启动 Flask。
- 覆盖浏览会话、搜索、打开详情、工具卡片、错误展示、移动端布局。

验收：

- `npm run test:e2e` 或等价命令可重复通过。
- CI 或本地脚本能一键运行后端测试和前端测试。

### 阶段 4.2：文档和部署流程

范围：

- 更新 README：Python/Node 安装、开发、构建、运行、测试、日志、配置。
- 增加故障排查：DB 路径、opencode CLI、限流、微信登录、Cloudflare Tunnel。

验收：

- 新用户按 README 能启动服务并访问页面。
- 开发者按 README 能运行测试和构建。

### 阶段 4.3：最终清理和兼容性验收

范围：

- 删除旧前端死代码和已迁移的重复后端逻辑。
- 全量跑测试。
- 按 #12 最终验收标准做完整浏览器自测。

验收：

- 原有功能全部可用：浏览、搜索、继续、新建、撤销、删除、对比、Provider 管理。
- 限流/错误 5 秒内显示清晰提示。
- 对话完成检测无明显误报/漏报。
- Web 端可实时同步外部 CLI 变化。
- 微信监控只在最终完成时通知。
- Flask 退出不残留 OpenCode 子进程。

## 5. 每阶段自测记录模板

每个阶段 Issue 评论使用以下模板：

```md
## 阶段结果

- 解决内容：
- 关键实现：
- 提交 SHA：
- PR：

## 自动化测试

- `python -m pytest`：
- `npm run test`：
- `npm run build`：

## 浏览器自测

- 首页加载：
- 搜索/过滤：
- 会话详情：
- 继续/新建会话：
- 工具调用展示：
- Provider 管理：
- 删除/撤销/Fork/对比：
- 移动端：

## 风险和后续

- 已知风险：
- 下一阶段：
```

## 6. 风险控制

- 不在同一阶段同时做“模块化 + 行为改变 + UI 改版”。
- 任何公共 API 字段变化必须先加兼容层，再迁移前端，最后清理旧字段。
- 对 OpenCode 事件的判断必须有 fixture 覆盖，尤其是 `tool-calls`、`stop`、错误、超时和断连。
- React 迁移期间保留旧入口，直到核心路径通过 E2E。
- 监控脚本和 Web 后端共享完成判断逻辑，避免两个地方各自实现不同规则。

## 7. 当前阶段后的下一步

完成阶段 0.1 后，下一阶段应优先做 **阶段 0.2：测试脚手架和最小 smoke 测试**。没有测试前不建议继续大规模拆分 `app.py` 或迁移前端，否则后续阶段会反复破坏已有功能。
